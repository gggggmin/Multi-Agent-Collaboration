# 算法模块说明

记录日期：2026-09-21

## 1. 当前项目已集成的算法

本项目不是只靠规则拼流程，当前包含三类可写入论文的方法算法。

### 1.1 RL-AGS：基于 PPO 的自适应博弈终止算法

位置：

- `framework/rl/ags_environment.py`
- `framework/rl/ags_trainer.py`
- `framework/multi_agent/coordinator.py`

RL-AGS 将 Creator-Auditor 多智能体博弈过程建模为 MDP：

| 要素 | 定义 |
|---|---|
| 状态 State | 当前迭代轮次、审计问题数、覆盖率估计、是否存在幻觉、是否审计通过 |
| 动作 Action | `0=继续迭代`，`1=接受当前输出并停止` |
| 奖励 Reward | 审计通过奖励、覆盖率收益、迭代成本惩罚、幻觉惩罚、超限惩罚 |
| 算法 | PPO，基于 Stable-Baselines3 |

观测向量为 5 维：

```text
[
  current_iteration / max_iterations,
  num_issues / 10,
  coverage_estimate,
  hallucination_flag,
  audit_passed_flag
]
```

动作空间为离散二元动作：

```text
0 = Continue
1 = Accept
```

该算法的作用不是生成测试用例，而是学习在多智能体审计过程中何时停止，避免无效迭代。

### 1.2 RL-HSS：基于 ε-greedy Bandit 的自愈策略选择算法

位置：

- `framework/rl/hss_bandit.py`
- `framework/self_healing/healing_agent.py`

RL-HSS 将脚本自愈策略选择建模为多臂赌博机问题。

| 要素 | 定义 |
|---|---|
| 上下文 | 错误类型，如 locator、timeout、assertion、syntax |
| 策略臂 | `llm_rewrite`、`rule_locator`、`rule_timeout`、`fallback` |
| 奖励 | 修复成功为 `1.0`，失败为 `-0.5` |
| 更新 | `Q_new = Q_old + alpha * (reward - Q_old)` |
| 选择 | ε-greedy，兼顾探索和利用 |

该算法的作用是根据历史修复效果动态选择自愈策略，而不是每次都调用成本更高的大模型重写。

### 1.3 RCP-TCP：需求覆盖优先的测试用例排序算法

位置：

- `framework/rl/test_prioritizer.py`
- `experiments/run_prioritization_experiment.py`
- `experiments/run_strategy_comparison.py`

RCP-TCP 用于解决“生成大量测试用例后先执行哪些”的问题。它不是替代用例生成，而是在执行前按照覆盖价值、风险和成本对用例排序，让高价值用例更早暴露问题。

评分函数为：

```text
Score(t) =
  w1 * RequirementCoverage(t)
  + w2 * RiskLevel(t)
  + w3 * HistoricalFailureRate(t)
  + w4 * BoundaryCaseFlag(t)
  - w5 * ExecutionCost(t)
```

当前默认权重：

| 特征 | 权重 |
|---|---:|
| RequirementCoverage | 0.35 |
| RiskLevel | 0.25 |
| HistoricalFailureRate | 0.20 |
| BoundaryCaseFlag | 0.10 |
| ExecutionCost | 0.10 |

最新排序实验：

```text
python experiments/run_prioritization_experiment.py
python experiments/run_strategy_comparison.py
```

输出文件：

```text
experiments/results/prioritization_latest.json
experiments/results/strategy_comparison_latest.json
```

最新结果显示，排序器从端到端报告中提取 80 条测试用例，并优先排列边界值、高风险与历史失败用例。与无排序、模块顺序排序和纯风险排序相比，RCP-TCP 在前 20% 执行窗口中将历史失败用例捕获率提升到 100.0%，同时保持 46.88% 的高风险用例捕获率和 83.33% 的边界值用例捕获率。

## 2. 最新 RL 模式运行报告

本次已使用 RL 模式运行完整智能体流水线。

报告文件：

```text
experiments/results/agent_pipeline_20260921_210235.json
```

关键结果：

| 指标 | 结果 |
|---|---:|
| RAG 语义切片数 | 80 |
| 生成用例数 | 80 |
| 覆盖率 | 100.0% |
| 幻觉率 | 0.0% |
| 是否启用 RL | true |
| 端到端执行 | 80 passed |

注意：本次报告证明 RL-AGS 已接入主流程，但由于干净需求中每个模块第 1 轮即审计通过，RL 的终止策略没有产生明显轮次差异。为展示算法价值，项目新增脏需求鲁棒性实验，并加入 Safety-gated RL-AGS：当 Auditor 检出 error 级问题时，即使 PPO 输出 accept，也强制改为 continue。

## 2.1 脏需求鲁棒性实验

报告文件：

```text
experiments/results/robustness_noisy_requirements_latest.json
```

实验注入四类问题：模糊需求、冲突需求、幻觉用例、不完整用例。最新结果：

| 类型 | 数量 |
|---|---:|
| ambiguous_requirement | 6 |
| conflicting_requirement | 2 |
| hallucination | 2 |
| incomplete | 2 |

原始 PPO 输出 `accept`，Safety-gated RL-AGS 输出 `continue`。该结果说明纯策略模型需要审计约束，安全门控可以阻止带 error 的用例集被错误接受。

## 2.2 终止策略对比实验

报告文件：

```text
experiments/results/strategy_comparison_latest.json
```

该实验构造 clean_passed_generation、noisy_conflict_and_hallucination、warning_only_incomplete_description 和 hallucination_error 四类审计状态，比较固定轮次、原始 PPO 与 Safety-gated RL-AGS。

| 策略 | 平均迭代轮次 | 不安全接受数 | 不安全接受率 |
|---|---:|---:|---:|
| 固定 1 轮 | 1.0 | 2 | 50.0% |
| 固定 3 轮 | 3.0 | 2 | 50.0% |
| 固定 5 轮 | 5.0 | 2 | 50.0% |
| 原始 PPO | 1.0 | 2 | 50.0% |
| Safety-gated RL-AGS | 1.0 | 0 | 0.0% |

结论：固定轮次只是拖时间，不会天然避免错误接受；原始 PPO 在样本不足时也可能冒进；Safety Gate 是必要的质量约束。

## 2.3 排序策略对比实验

| 策略 | 前 20% 高风险捕获率 | 前 20% 边界值捕获率 | 前 20% 历史失败捕获率 |
|---|---:|---:|---:|
| 无排序 | 34.38% | 27.78% | 0.0% |
| 模块顺序排序 | 12.50% | 16.67% | 0.0% |
| 风险排序 | 50.00% | 88.89% | 0.0% |
| RCP-TCP | 46.88% | 83.33% | 100.0% |

结论：RCP-TCP 不是单纯按边界值排序，而是把历史失败反馈纳入排序。它略微牺牲纯风险捕获率，换来历史失败用例优先执行，适合持续集成里的快速反馈场景。

## 3. 当前不足

现在的算法线已经可以撑起独立章节，但还需要用实验继续压实。

1. RL-AGS 的训练环境仍偏模拟，真实博弈反馈样本不足。
2. 当前需求质量较好，Creator 第 1 轮基本通过，RL 的决策优势不明显。
3. RL-HSS 目前主要在自愈实验中验证定位器类错误，故障类型还需要扩展。
4. RCP-TCP 当前完成排序验证，但还需要补 APFD 或前 N% 缺陷发现率对比，才能证明排序收益。
5. 若论文强调“深度学习”，需要把 PPO 训练曲线、策略对比和多轮失败样本补出来。

## 4. 论文建议表述

建议把论文核心方法表述为：

```text
本文提出一种 RAG 增强的多智能体自动化测试生成框架，
并引入基于 PPO 的自适应博弈终止策略 RL-AGS 与基于 Bandit 的自愈策略选择算法 RL-HSS，
同时设计 RCP-TCP 测试用例优先级排序算法，
用于优化用例生成迭代成本、脚本修复策略选择与测试执行顺序。
```

不要写成：

```text
本文提出一种端到端深度学习测试生成模型。
```

后者不准确，容易被答辩老师抓住锤。
