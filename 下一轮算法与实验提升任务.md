# 下一轮算法与实验提升任务

> 生成时间：2026-09-22  
> 用途：关闭当前会话后，在下一个对话中继续执行。  
> 当前状态：算法章已补强，新增 RCP-TCP 排序算法与策略对比实验，但仍需继续压实 PPO 训练证据、排序指标和论文一致性。

## 1. 本轮已完成内容

### 1.1 论文结构调整

已将论文从原来的“系统设计里夹带算法小节”调整为：

```text
第3章 系统设计与实现
第4章 核心算法设计
第5章 实验与结果分析
第6章 总结与展望
```

已修改文件：

```text
论文/硕士论文初稿.md
论文框架.md
算法模块说明.md
```

### 1.2 新增算法

新增 RCP-TCP 测试用例优先级排序算法：

```text
framework/rl/test_prioritizer.py
```

核心评分函数：

```text
Score(t) =
  w1 * RequirementCoverage(t)
  + w2 * RiskLevel(t)
  + w3 * HistoricalFailureRate(t)
  + w4 * BoundaryCaseFlag(t)
  - w5 * ExecutionCost(t)
```

### 1.3 新增实验脚本

新增排序实验：

```text
experiments/run_prioritization_experiment.py
```

新增策略对比实验：

```text
experiments/run_strategy_comparison.py
```

### 1.4 已生成实验结果

```text
experiments/results/prioritization_latest.json
experiments/results/strategy_comparison_latest.json
```

当前关键结果：

```text
Safety-gated RL-AGS:
固定1轮 / 固定3轮 / 固定5轮 / 原始PPO：不安全接受率 50.0%
Safety-gated RL-AGS：不安全接受率 0.0%

RCP-TCP:
前20%高风险捕获率：46.88%
前20%边界值捕获率：83.33%
前20%历史失败捕获率：100.0%
```

已通过验证：

```bash
python -m py_compile framework\rl\__init__.py framework\rl\test_prioritizer.py experiments\run_prioritization_experiment.py experiments\run_strategy_comparison.py
python experiments\run_strategy_comparison.py
```

运行 `run_strategy_comparison.py` 时会出现 Stable-Baselines3 的 GPU/CPU 警告，不影响结果。

## 2. 下一轮必须继续提升的问题

### 2.1 PPO 训练证据仍偏弱

当前问题：

```text
RL-AGS 已经接入 Coordinator，也有模型文件 .cache/rl_models/ags_ppo.zip。
但是训练环境仍偏模拟，真实 Creator-Auditor 轨迹不足。
```

风险：

答辩老师可能会问：

```text
你的 PPO 到底学到了什么？
为什么不是规则 if-else 就够了？
```

下一步建议：

1. 新增真实/半真实审计轨迹数据集。
2. 记录每轮状态：

```text
iteration
num_issues
num_error_issues
coverage_estimate
has_hallucination
audit_passed
action
reward
```

3. 生成 PPO 训练曲线或至少生成 reward trend JSON。
4. 在论文中明确写：

```text
PPO 用于学习终止倾向，Safety Gate 用于保证质量底线。
```

不要硬吹“PPO 本身保证质量”，这话不稳。

### 2.2 RL-AGS 对比实验还可以更硬

当前已有结果：

```text
固定轮次、原始 PPO、Safety-gated RL-AGS 在 4 类审计状态上做了不安全接受率对比。
Safety-gated RL-AGS 把不安全接受率从 50.0% 降到 0.0%。
```

不足：

当前是策略状态对比，不是完整多轮真实生成对比。

下一步建议新增：

```text
experiments/run_rl_iteration_benchmark.py
```

建议输出：

```text
experiments/results/rl_iteration_benchmark_latest.json
```

对比策略：

```text
fixed_1_round
fixed_3_round
fixed_5_round
raw_ppo
safety_gated_rl_ags
```

指标：

```text
avg_iterations
accepted_cases
unsafe_accepts
unsafe_accept_rate_pct
estimated_llm_calls
estimated_cost_reduction_pct
```

### 2.3 RCP-TCP 排序实验还缺 APFD

当前已有结果：

```text
RCP-TCP 可以把历史失败用例 TC-SEARCH-005 提升到前 20%。
```

不足：

目前只有前 20% 捕获率，没有 APFD 指标。测试优先级排序论文里 APFD 很常见，不补有点亏。

下一步建议：

在 `experiments/run_strategy_comparison.py` 中加入 APFD 计算：

```text
APFD = 1 - (sum(TF_i) / (n * m)) + 1 / (2n)
```

其中：

```text
n = 测试用例总数
m = 缺陷总数
TF_i = 第 i 个缺陷首次被发现时的用例位置
```

当前可以先把 `TC-SEARCH-005` 视为一个历史缺陷触发用例，计算单缺陷 APFD。

### 2.4 RCP-TCP 与风险排序的差异需要继续扩大

当前现象：

```text
风险排序：
前20%高风险捕获率 50.00%
前20%边界值捕获率 88.89%
历史失败捕获率 0.0%

RCP-TCP：
前20%高风险捕获率 46.88%
前20%边界值捕获率 83.33%
历史失败捕获率 100.0%
```

解释：

RCP-TCP 为了提前历史失败用例，略微牺牲了纯风险捕获率。这是合理的，但论文里要说清楚，别吹成所有指标全面碾压。

下一步建议：

1. 增加更多历史失败样本，例如：

```text
TC-SEARCH-005 locator conflict
TC-CHECKOUT-003 required address bypass
TC-CART-003 stock boundary
```

2. 让 RCP-TCP 的历史反馈优势更明显。
3. 补一个“排序前后 Top-10 用例列表”表格。

### 2.5 RL-HSS 需要补多故障类型实验

当前问题：

```text
自愈实验主要验证 locator 错误。
```

风险：

RL-HSS 如果只测一种 locator，Bandit 的策略选择价值不明显。

下一步建议构造 4 类故障：

```text
locator: 宽泛选择器导致 strict mode conflict
timeout: 等待时间过短
assertion: 页面文案断言变化
syntax: 生成脚本语法错误
```

新增脚本建议：

```text
experiments/run_healing_strategy_benchmark.py
```

输出：

```text
experiments/results/healing_strategy_benchmark_latest.json
```

指标：

```text
strategy
error_category
attempts
success
success_rate_pct
avg_attempts
selected_strategy_distribution
```

### 2.6 论文摘要和创新点需要最终统一

当前论文已经提到了 RCP-TCP，但还需要最终统一：

```text
摘要
Abstract
1.3 研究内容与创新点
第4章算法描述
第5章实验表格
第6章总结
```

下一步检查重点：

1. 是否所有章节都使用同一套算法名称：

```text
Safety-gated RL-AGS
RL-HSS
RCP-TCP
```

2. 是否避免夸大：

```text
不要写：PPO 显著提升测试质量
建议写：Safety-gated RL-AGS 降低高风险状态下的不安全接受率
```

3. 是否把实验文件路径全部写进附录。

## 3. 下个会话建议直接执行的命令

先检查当前状态：

```bash
git status --short
```

编译检查：

```bash
python -m py_compile framework\rl\__init__.py framework\rl\test_prioritizer.py experiments\run_prioritization_experiment.py experiments\run_strategy_comparison.py
```

重跑排序实验：

```bash
python experiments\run_prioritization_experiment.py
```

重跑策略对比实验：

```bash
python experiments\run_strategy_comparison.py
```

建议下一轮优先新增：

```bash
experiments/run_rl_iteration_benchmark.py
experiments/run_healing_strategy_benchmark.py
```

## 4. 下个会话可直接复制的任务提示词

```text
请继续根据《下一轮算法与实验提升任务.md》执行。
优先完成：
1. 新增 RL-AGS 多策略迭代基准实验 run_rl_iteration_benchmark.py；
2. 给 RCP-TCP 排序实验补 APFD 指标；
3. 新增 RL-HSS 多故障类型自愈策略基准实验 run_healing_strategy_benchmark.py；
4. 将新增实验结果写回 论文/硕士论文初稿.md 和 算法模块说明.md。
要求：代码要可运行，生成 latest JSON 结果，论文表格必须引用真实输出数据，不要编数据。
```

## 5. 关键判断

目前毕设算法线已经能撑起独立章节，但还没到“特别硬”的程度。真正需要补强的是实验闭环：

```text
算法定义 -> 可运行实现 -> JSON 结果 -> 论文表格 -> 局限性说明
```

别再只堆算法名。算法名再多，没有实验数据就是纸糊的，答辩时一问就露。
